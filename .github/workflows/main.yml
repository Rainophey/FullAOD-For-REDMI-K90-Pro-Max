name: Package Magisk Module

on:
  workflow_dispatch: 

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Upload Artifact to CI
        uses: actions/upload-artifact@v6
        with:
          name: 816faod_$(date +'%Y%m%d_%H%M')
          path: |
            ./*
            !.git*
            !.github*
            !README.md
          retention-days: 90
